# Migrations

This folder contains migration files for setting up the database schema.

### Migration Files
1. `001_create_users_table.sql`: Creates the `users` table.
2. `002_create_notes_table.sql`: Creates the `notes` table and defines indexes for search functionality.

### Usage
Run the migration files sequentially using your PostgreSQL migration tool of choice.