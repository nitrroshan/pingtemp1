/**
 * Notification Tools — DEPRECATED
 *
 * The check_notifications tool is no longer needed.
 * Planner receives task events via onPlannerInput callback (new turns),
 * batched by NotificationQueue (100ms debounce).
 *
 * This file is kept for reference. The tool is not registered.
 */
